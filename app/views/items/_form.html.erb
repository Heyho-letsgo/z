<%= form_for(@item) do |f| %>
   <p>
          
          <%= f.label :title %><br />
          <%= f.text_field :title %>
        </p>
        <p>
          <%= f.label :body %><br />
          <%= f.text_area :body %>
        </p>
        <p>
    <%= f.submit "Créer et rediriger sur item"  %> 

  </p>
<% end %>
    <%= link_to "Accueuil", items_path() %>
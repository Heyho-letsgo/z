<h1>Affiche tout les éléments de la Table Article</h1>


	<% @items.each do |item| %>
		<li>
			<%=  link_to item.id, item_path(item) %> | <%= link_to item.title,item_path(item) %> | <%= item.body %><br>
		</li>
	<%end%> <br>


<%= link_to "nouvelle fiche", new_item_path()%>
